require "spec_helper"
require_relative "../lib/note"
require_relative "../lib/white_key"
require_relative "../lib/white_keys"

describe WhiteKey do
  # describe "#-" do
    # it "returns the WhiteKey that corresponds to the given Note" do
      # new_white_key = WhiteKeys[:c] - Note.new(:f, :sharp)
      # expect(new_white_key).to eq(WhiteKey.new(:f, :sharp))
    # end
  # end
end
